include kitty.conf

# Small type and tight padding: btop needs 24 rows to draw at all, and the process
# table is the point of the window, so fit as many rows as the tile allows
font_size 11
window_padding_width 4

# Nothing to lose on close, so no "are you sure"
confirm_os_window_close 0
